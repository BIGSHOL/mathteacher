# TRD (기술 요구사항 정의서)

> 개발자/AI 코딩 파트너가 참조하는 기술 문서입니다.
> 기술 표현을 사용하되, "왜 이 선택인지"를 함께 설명합니다.

---

## MVP 캡슐

| # | 항목 | 내용 |
|---|------|------|
| 1 | 목표 | 강사 효율 극대화 + 학생 자기주도 학습 환경 구축 |
| 2 | 페르소나 | 초등~고1 학생 (공통수학), 학원 강사/관리자 |
| 3 | 핵심 기능 | FEAT-1: 개념 테스트 (수학 개념 이해도 확인) |
| 4 | 성공 지표 (노스스타) | 강사 개별 확인 시간 50% 절감 |
| 5 | 입력 지표 | 주간 테스트 완료 학생 비율, 평균 정답률 향상 |
| 6 | 비기능 요구 | 응답 시간 < 500ms, 모바일/태블릿 반응형 지원 |
| 7 | Out-of-scope | AI 개인 맞춤 추천, 학부모 앱, 결제 시스템 |
| 8 | Top 리스크 | 학생들이 재미없어서 안 쓸 수 있음 |
| 9 | 완화/실험 | 듀오링고 스타일 게임화 요소 적용 |
| 10 | 다음 단계 | 개념 테스트 문제 DB 설계 |

---

## 1. 시스템 아키텍처

### 1.1 고수준 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           사용자 디바이스                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                      │
│  │   모바일     │  │   태블릿     │  │     PC      │                      │
│  │  (학생/집)   │  │  (학생/랩실) │  │  (강사/관리) │                      │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘                      │
└─────────┼────────────────┼────────────────┼─────────────────────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │ HTTPS
                           ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        Frontend (React + Vite)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  학생 앱     │  │  강사 대시보드│  │  게임화 UI   │  │  반응형 레이아웃│   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└────────────────────────────┬────────────────────────────────────────────┘
                             │ REST API
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        Backend (FastAPI)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  인증 서비스  │  │ 테스트 서비스 │  │ 채점 서비스  │  │ 통계 서비스   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        Database (PostgreSQL)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │  사용자 데이터 │  │   문제 뱅크   │  │  학습 기록   │  │  통계 데이터  │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 컴포넌트 설명

| 컴포넌트 | 역할 | 왜 이 선택? |
|----------|------|-------------|
| Frontend (React + Vite) | 학생/강사 인터페이스, 게임화 UI | 듀오링고 스타일 애니메이션 구현에 적합, 빠른 개발 |
| Backend (FastAPI) | API 서버, 비즈니스 로직, 인증 | Python 기반 빠른 개발, 자동 API 문서, 학습 통계 분석 용이 |
| Database (PostgreSQL) | 문제/학습 기록/사용자 저장 | 복잡한 통계 쿼리 지원, 확장성, 비용 예측 가능 |

---

## 2. 권장 기술 스택

### 2.1 프론트엔드

| 항목 | 선택 | 이유 | 벤더 락인 리스크 |
|------|------|------|-----------------|
| 프레임워크 | React 18 + Vite | 게임화 애니메이션 구현에 적합, 넓은 생태계 | 낮음 |
| 언어 | TypeScript | 타입 안정성, 버그 사전 방지 | - |
| 스타일링 | TailwindCSS | 빠른 스타일링, 반응형 쉬움, 커스터마이징 용이 | 낮음 |
| 상태관리 | Zustand | 가볍고 간단, Redux 대비 보일러플레이트 적음 | 낮음 |
| HTTP 클라이언트 | Axios | 인터셉터 지원, 에러 핸들링 편리 | 낮음 |
| 애니메이션 | Framer Motion | 게임화 피드백 애니메이션 (레벨업, 콤보 등) | 낮음 |
| 아이콘 | Lucide React | 깔끔한 라인 아이콘, 트리쉐이킹 지원 | 낮음 |

### 2.2 백엔드

| 항목 | 선택 | 이유 | 벤더 락인 리스크 |
|------|------|------|-----------------|
| 프레임워크 | FastAPI | 빠른 개발, 자동 API 문서 (Swagger), 비동기 지원 | 낮음 |
| 언어 | Python 3.11+ | 초보자 친화적, 데이터 처리에 강함 | - |
| ORM | SQLAlchemy 2.0 | Python 표준, 복잡한 쿼리 지원, 마이그레이션 | 낮음 |
| 검증 | Pydantic v2 | 요청/응답 검증, FastAPI 통합 | 낮음 |
| 마이그레이션 | Alembic | SQLAlchemy 표준 마이그레이션 도구 | 낮음 |

### 2.3 데이터베이스

| 항목 | 선택 | 이유 |
|------|------|------|
| 메인 DB | PostgreSQL 15+ | 학생별 통계 분석에 강력, 복잡한 쿼리 지원, JSON 타입 |
| 캐시 | Redis (v2) | MVP에서는 제외, 성능 이슈 시 추가 |

### 2.4 인프라

| 항목 | 선택 | 이유 |
|------|------|------|
| 컨테이너 | Docker + Docker Compose | 로컬 개발 일관성, 배포 환경 통일 |
| 호스팅 (Frontend) | Vercel / Netlify | 무료 티어, CDN, 자동 배포 |
| 호스팅 (Backend) | Railway / Render | PostgreSQL 통합, 무료/저가 시작 가능 |
| CI/CD | GitHub Actions | 무료, 테스트/배포 자동화 |

---

## 3. 비기능 요구사항

### 3.1 성능

| 항목 | 요구사항 | 측정 방법 |
|------|----------|----------|
| API 응답 시간 | < 500ms (P95) | FastAPI 로깅, 모니터링 |
| 초기 로딩 | < 3s (FCP) | Lighthouse |
| 채점 응답 | < 200ms | 즉시 피드백을 위해 빠른 응답 필수 |

### 3.2 보안

| 항목 | 요구사항 |
|------|----------|
| 인증 | JWT + Refresh Token |
| 비밀번호 | bcrypt 해싱 (강사 계정) |
| 학생 계정 | 단순 PIN 또는 학생 ID + 비밀번호 |
| HTTPS | 필수 |
| 입력 검증 | 서버 측 Pydantic 검증 필수 |

### 3.3 확장성

| 항목 | 현재 (MVP) | 목표 (v2) |
|------|------|------|
| 동시 사용자 | 50명 (학원 1개) | 500명 (학원 10개) |
| 문제 수 | 500문제 | 5,000문제 |
| 데이터 용량 | 1GB | 10GB |

---

## 4. 외부 API 연동

### 4.1 인증

| 서비스 | 용도 | 필수/선택 | 연동 방식 |
|--------|------|----------|----------|
| 없음 (자체 인증) | 학원 전용 계정 | 필수 | JWT |

### 4.2 기타 서비스

| 서비스 | 용도 | 필수/선택 | 비고 |
|--------|------|----------|------|
| 이미지 저장 (v2) | 문제 이미지 | 선택 | Cloudinary / S3 |
| 푸시 알림 (v2) | 학습 리마인더 | 선택 | Firebase FCM |

---

## 5. 접근제어·권한 모델

### 5.1 역할 정의

| 역할 | 설명 | 권한 |
|------|------|------|
| Student | 학생 | 테스트 풀기, 본인 기록 조회 |
| Teacher | 강사 | 학생 관리, 전체 기록 조회, 테스트 출제 |
| Admin | 관리자 | 전체 시스템 관리, 강사 계정 생성 |

### 5.2 권한 매트릭스

| 리소스 | Student | Teacher | Admin |
|--------|---------|---------|-------|
| 테스트 풀기 | O | O | O |
| 본인 기록 조회 | O | O | O |
| 학생 목록 조회 | - | O (담당 반) | O |
| 학생 기록 조회 | - | O (담당 반) | O |
| 학생 계정 생성 | - | O | O |
| 문제 관리 | - | O | O |
| 강사 계정 생성 | - | - | O |

---

## 6. 데이터 생명주기

### 6.1 원칙

- **최소 수집**: 학습에 필요한 데이터만 수집
- **명시적 동의**: 개인정보 최소화 (이름, 학년 정도)
- **보존 기한**: 학원 정책에 따름

### 6.2 데이터 흐름

```
수집 → 저장 → 사용 → 보관 → 삭제/익명화
```

| 데이터 유형 | 보존 기간 | 삭제/익명화 |
|------------|----------|------------|
| 학생 계정 정보 | 퇴원 후 1년 | 완전 삭제 |
| 학습 기록 | 영구 | 퇴원 시 익명화 |
| 테스트 결과 | 영구 | 통계 분석용 유지 |

---

## 7. 테스트 전략 (Contract-First TDD)

### 7.1 개발 방식: Contract-First Development

본 프로젝트는 **계약 우선 개발(Contract-First Development)** 방식을 채택합니다.
BE/FE가 독립적으로 병렬 개발하면서도 통합 시 호환성을 보장합니다.

```
┌─────────────────────────────────────────────────────────────┐
│                    Contract-First 흐름                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 계약 정의 (Phase 0)                                     │
│     ├─ API 계약: contracts/*.contract.ts                   │
│     ├─ BE 스키마: backend/app/schemas/*.py                 │
│     └─ 타입 동기화: TypeScript ↔ Pydantic                  │
│                                                             │
│  2. 테스트 선행 작성 (RED)                                  │
│     ├─ BE 테스트: tests/api/*.py                           │
│     ├─ FE 테스트: src/__tests__/**/*.test.ts               │
│     └─ 모든 테스트가 실패하는 상태 (정상!)                  │
│                                                             │
│  3. Mock 생성 (FE 독립 개발용)                              │
│     └─ MSW 핸들러: src/mocks/handlers/*.ts                 │
│                                                             │
│  4. 병렬 구현 (RED→GREEN)                                   │
│     ├─ BE: 테스트 통과 목표로 구현                          │
│     └─ FE: Mock API로 개발 → 나중에 실제 API 연결          │
│                                                             │
│  5. 통합 검증                                               │
│     └─ Mock 제거 → E2E 테스트                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 테스트 피라미드

| 레벨 | 도구 | 커버리지 목표 | 위치 |
|------|------|-------------|------|
| Unit | pytest / Vitest | ≥ 80% | tests/unit/, src/__tests__/ |
| Integration | pytest / Vitest + MSW | Critical paths | tests/integration/ |
| E2E | Playwright | Key user flows | e2e/ |

### 7.3 테스트 도구

**백엔드:**
| 도구 | 용도 |
|------|------|
| pytest | 테스트 실행 |
| pytest-asyncio | 비동기 테스트 |
| httpx | API 클라이언트 (TestClient 대체) |
| Factory Boy | 테스트 데이터 생성 |
| pytest-cov | 커버리지 측정 |

**프론트엔드:**
| 도구 | 용도 |
|------|------|
| Vitest | 테스트 실행 |
| React Testing Library | 컴포넌트 테스트 |
| MSW (Mock Service Worker) | API 모킹 |
| Playwright | E2E 테스트 |

### 7.4 계약 파일 구조

```
project/
├── contracts/                    # API 계약 (BE/FE 공유)
│   ├── types.ts                 # 공통 타입 정의
│   ├── auth.contract.ts         # 인증 API 계약
│   ├── test.contract.ts         # 테스트 API 계약
│   └── stats.contract.ts        # 통계 API 계약
│
├── backend/
│   ├── app/schemas/             # Pydantic 스키마 (계약과 동기화)
│   │   ├── auth.py
│   │   ├── test.py
│   │   └── stats.py
│   └── tests/
│       └── api/                 # API 테스트 (계약 기반)
│           ├── test_auth.py
│           ├── test_test.py
│           └── test_stats.py
│
└── frontend/
    ├── src/
    │   ├── mocks/
    │   │   ├── handlers/        # MSW Mock 핸들러
    │   │   │   ├── auth.ts
    │   │   │   └── test.ts
    │   │   └── data/            # Mock 데이터
    │   └── __tests__/
    │       └── api/             # API 테스트 (계약 기반)
    └── e2e/                     # E2E 테스트
```

### 7.5 TDD 사이클

모든 기능 개발은 다음 사이클을 따릅니다:

```
RED    → 실패하는 테스트 먼저 작성 (M0.5에서 완료)
GREEN  → 테스트를 통과하는 최소한의 코드 구현
REFACTOR → 테스트 통과 유지하며 코드 개선
```

### 7.6 품질 게이트

**병합 전 필수 통과:**
- [ ] 모든 단위 테스트 통과
- [ ] 커버리지 ≥ 80%
- [ ] 린트 통과 (ruff / ESLint)
- [ ] 타입 체크 통과 (mypy / tsc)
- [ ] E2E 테스트 통과 (해당 기능)

**검증 명령어:**
```bash
# 백엔드
pytest --cov=app --cov-report=term-missing
ruff check .
mypy app/

# 프론트엔드
npm run test -- --coverage
npm run lint
npm run type-check

# E2E
npx playwright test
```

---

## 8. API 설계 원칙

### 8.1 RESTful 규칙

| 메서드 | 용도 | 예시 |
|--------|------|------|
| GET | 조회 | GET /api/v1/tests/{id} |
| POST | 생성 | POST /api/v1/tests |
| PUT | 전체 수정 | PUT /api/v1/tests/{id} |
| PATCH | 부분 수정 | PATCH /api/v1/tests/{id} |
| DELETE | 삭제 | DELETE /api/v1/tests/{id} |

### 8.2 주요 API 엔드포인트

**인증 (FEAT-0):**
```
POST   /api/v1/auth/login          # 로그인
POST   /api/v1/auth/refresh        # 토큰 갱신
POST   /api/v1/auth/logout         # 로그아웃
```

**학생 관리:**
```
GET    /api/v1/students            # 학생 목록 (강사용)
POST   /api/v1/students            # 학생 생성 (강사용)
GET    /api/v1/students/{id}       # 학생 상세
PATCH  /api/v1/students/{id}       # 학생 수정
```

**테스트 (FEAT-1):**
```
GET    /api/v1/tests/available     # 풀 수 있는 테스트 목록
GET    /api/v1/tests/{id}          # 테스트 상세 (문제 포함)
POST   /api/v1/tests/{id}/submit   # 답안 제출 + 채점
GET    /api/v1/tests/{id}/result   # 결과 조회
```

**통계:**
```
GET    /api/v1/stats/me            # 내 학습 통계
GET    /api/v1/stats/students      # 학생별 통계 (강사용)
GET    /api/v1/stats/concepts      # 개념별 정답률
```

### 8.3 응답 형식

**성공 응답:**
```json
{
  "data": { ... },
  "meta": {
    "page": 1,
    "total": 100
  }
}
```

**에러 응답:**
```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "입력값이 올바르지 않습니다.",
    "details": [
      { "field": "answer", "message": "답을 선택해주세요" }
    ]
  }
}
```

### 8.4 API 버저닝

| 방식 | 예시 | 채택 여부 |
|------|------|----------|
| URL 경로 | /api/v1/tests | 채택 |

---

## 9. 병렬 개발 지원 (Git Worktree)

### 9.1 개요

BE/FE를 완전히 독립된 환경에서 병렬 개발할 때 Git Worktree를 사용합니다.

### 9.2 Worktree 구조

```
~/projects/
├── math-test/                 # 메인 (main 브랜치)
├── math-test-auth-be/         # Worktree: feature/auth-be
├── math-test-auth-fe/         # Worktree: feature/auth-fe
├── math-test-test-be/         # Worktree: feature/test-be
└── math-test-test-fe/         # Worktree: feature/test-fe
```

### 9.3 명령어

```bash
# Worktree 생성
git worktree add ../math-test-auth-be -b feature/auth-be
git worktree add ../math-test-auth-fe -b feature/auth-fe

# 각 Worktree에서 독립 작업
cd ../math-test-auth-be && pytest tests/api/test_auth.py
cd ../math-test-auth-fe && npm run test -- src/__tests__/auth/

# 테스트 통과 후 병합
git checkout main
git merge --no-ff feature/auth-be
git merge --no-ff feature/auth-fe

# Worktree 정리
git worktree remove ../math-test-auth-be
git worktree remove ../math-test-auth-fe
```

### 9.4 병합 규칙

| 조건 | 병합 가능 |
|------|----------|
| 단위 테스트 통과 (GREEN) | 필수 |
| 커버리지 ≥ 80% | 필수 |
| 린트/타입 체크 통과 | 필수 |
| E2E 테스트 통과 | 권장 |

---

## Decision Log 참조

| ID | 항목 | 선택 | 근거 | 영향 |
|----|------|------|------|------|
| D-05 | 백엔드 | FastAPI | 빠른 개발, Python 기반, 자동 API 문서 | API 설계, 테스트 도구 선택 |
| D-06 | 프론트엔드 | React + Vite | 게임화 애니메이션 구현, 넓은 생태계 | UI 컴포넌트 구조 |
| D-07 | 데이터베이스 | PostgreSQL | 복잡한 통계 쿼리, 확장성 | 스키마 설계, 쿼리 최적화 |
| D-08 | 인증 | JWT + 자체 계정 | 학원 전용, 외부 의존 없음 | 소셜 로그인 제외 |
