# ===========================================
# Math Test API - Environment Variables
# ===========================================
# Copy this file to .env and fill in the values

# Environment: development, staging, production
# CRITICAL: Set to "production" in production environments
ENV=development

# Database URL
# CRITICAL: Use strong credentials in production
# Supabase: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# SQLite (dev only): sqlite:///./math_test.db
DATABASE_URL=

# JWT Secret Key
# CRITICAL: Must be at least 32 characters
# Generate with: openssl rand -hex 32
JWT_SECRET=

# CORS Origins (JSON array format)
# Production example: ["https://your-app.vercel.app"]
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# ===========================================
# Production Settings (Railway)
# ===========================================
# ENV=production
# DATABASE_URL=postgresql://postgres.[ref]:[pwd]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres
# JWT_SECRET=<openssl rand -hex 32>
# BACKEND_CORS_ORIGINS=["https://your-app.vercel.app"]

# ===========================================
# Production Checklist:
# ===========================================
# [ ] Set ENV=production
# [ ] Supabase: Copy connection string (Settings > Database > Connection string > URI)
# [ ] Generate secure JWT_SECRET: openssl rand -hex 32
# [ ] Update CORS origins for your Vercel domain
# [ ] Railway: Add all env vars in Variables tab
